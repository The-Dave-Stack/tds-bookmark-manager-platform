# Environment variables for the 'test' mode (Vitest)

# Ensure that all tests use the mock API service by default.
VITE_USE_MOCK_API=true

# You can also override other variables here if needed for tests.
# VITE_API_URL=http://mock-server.test