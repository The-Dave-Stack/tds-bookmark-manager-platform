{
  "name": "@tds/tds-bookmark-manager-api",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend/tds-bookmark-manager-api/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["node-env=production"]
      },
      "configurations": {
        "development": {
          "args": ["node-env=development"]
        },
        "docker": {
          "args": ["node-env=docker"]
        }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "@tds/tds-bookmark-manager-api:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "@tds/tds-bookmark-manager-api:build:development"
        },
        "docker": {
          "buildTarget": "@tds/tds-bookmark-manager-api:build:docker",
          "watch": false
        },
        "production": {
          "buildTarget": "@tds/tds-bookmark-manager-api:build:production",
          "watch": false
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    }
  }
}
